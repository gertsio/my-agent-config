---
argument-hint: <what to write, plus platform or format if useful>
---

# Writing Alias

Use the `writing` skill for this request.

- Treat `$ARGUMENTS` as the writing brief.
- Load `../skills/writing/private/skills/writing/profile.md` if it exists.
- Return only the final draft unless the user asks for alternatives or notes.
